package modmaker.gui;

import modmaker.Start;


public class OpenModInfo {
	public static boolean done;
	public static void open(String title, boolean replace){
		done = false;
		Start.gui.modInfo.popUpFrame(title,reaplace);
		while(!done){
			
		}
	}
}
