package modmaker.gui;

import org.lwjgl.opengl.GL11;

public class Button {
	private int x,y,lenght,width;
	private Icon[] icon;
	private ButtonAction call;
	private int state = 0;
	public Button(int x, int y,int lenght, int width, Icon off, Icon offActive, Icon on, Icon onActive, ButtonAction call){
		this.x = x;
		this.y = y;
		icon = new Icon[]{off,offActive,on,onActive};
	}
	public void upDate(int x,int y){
		if(x- this.x > 0 && x- this.x < width){
			if(y - this.y > 0 && y- this.y < width)
		}
	}
	public void render(){
		GL11.glPushMatrix();
		GL11.glTranslated(x, y, 0);
		GL11.glPopMatrix();
	}
}
